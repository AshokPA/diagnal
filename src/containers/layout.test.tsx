import { render, screen } from '@testing-library/react';
import Layout from './layout';
describe('Layout', () => {
    test('render pageTitle', async () => {
        render(<Layout />);
        
        
    });
});